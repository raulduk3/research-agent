# Origin of the owner app's /api/v1, for example https://owner-api.example.org.
# Empty means same origin. It must be served with credentialed CORS for the
# origin this build is published at (the launch profile's front_end_origin).
VITE_API_ORIGIN=
